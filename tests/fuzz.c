#include "rbtree.h"

/* Placeholder main: lets `make` link build/fuzz successfully so
 * src/rbtree.c's compilation can be checked before the real fuzz
 * harness is written. */
int main(int argc, char *argv[])
{
    (void)argc;
    (void)argv;
    return 0;
}